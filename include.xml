<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="mac_utils" unless="android" />

	<!-- Use the following for an Android Java extension, not needed otherwise -->
	
	<dependency name="mac_utils" path="dependencies/android" if="android" />
	<android extension="org.haxe.extension.Mac_utils" />
	
</project>
